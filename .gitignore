# Rust
/target
**/*.rs.bk
*.pdb
# Cargo.lock IS committed (this workspace ships the `ost` binary).

# Nix build symlinks
/result
/result-*

# Editor / OS
.DS_Store
Thumbs.db
*.swp
*~
*.bak
*.tmp
.idea/
.vscode/

# Logs / runtime
*.log
nohup.out

# Local env / secrets
.env
.env.*

# Local secrets / runtime state (NEVER commit)
config.toml
*.local.toml
/.cache/
throttle.json

# Test/scratch subtitle artifacts
*.srt
*.ass
*.ssa
*.vtt
*.sub
!tests/**/fixtures/**    # keep checked-in test fixtures

# Research clones (kept out of the repo on purpose)
/tmp-research/
